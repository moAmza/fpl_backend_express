interface TeamControllerInterface {
  getTeam: import("express").RequestHandler;
  addPlayer: import("express").RequestHandler;
  removePlayer: import("express").RequestHandler;
  addPlayer: import("express").RequestHandler;
  swapPlayers: import("express").RequestHandler;
}
